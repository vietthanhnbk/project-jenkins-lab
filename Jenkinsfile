pipeline{
  agent none
  options{
        skipDefaultCheckout()
  }
  stages{
    stage('BUILD' ){
      agent any
      options{
        skipDefaultCheckout()
      }
      steps{
        echo 'hello world 2'
      }
    }
  }
}
